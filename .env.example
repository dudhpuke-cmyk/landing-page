# Database Configuration
# Required if using database features
# Format: postgresql://user:password@host:port/database
DATABASE_URL="postgresql://user:password@localhost:5432/dudhpuke"

# Authentication (NextAuth)
# Required if using authentication features
# Generate a random secret: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here"
NEXTAUTH_URL="http://localhost:3000"

# Resend Email API
# Required for email functionality (contact form, subscription inquiries)
# Get your API key from https://resend.com/api-keys
RESEND_API_KEY="re_your_api_key_here"

# Resend From Email
# Email address to send emails from (must be verified in Resend)
# For testing: onboarding@resend.dev
# For production: no-reply@yourdomain.com (verify domain first)
RESEND_FROM_EMAIL="onboarding@resend.dev"

# Admin Email
# Email address to receive contact form submissions and inquiries
ADMIN_EMAIL="dudhpuke@gmail.com"

# Site URL
# Your production domain URL (used for SEO metadata, OpenGraph, etc.)
# For local development: http://localhost:3000
# For production: https://www.dudhpuke.com
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Node Environment
# Automatically set by most deployment platforms
# Options: development, production, test
NODE_ENV="development"
