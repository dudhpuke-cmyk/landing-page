# Database Configuration
# Optional: If not set, the app will run without database functionality
# Format: postgresql://user:password@host:port/database
DATABASE_URL="postgresql://user:password@localhost:5432/dudhpuke"

# Authentication (NextAuth)
# Required if using authentication features
# Generate a secure random string for production
NEXTAUTH_SECRET="your-secret-key-here-generate-a-secure-random-string"
NEXTAUTH_URL="http://localhost:3000"

# Resend Email API
# Required for email functionality (contact forms, subscription inquiries)
# Get your API key from https://resend.com/api-keys
RESEND_API_KEY="your-resend-api-key-here"

# Email Configuration (Optional)
# Override default email sender
# For production: Verify your domain in Resend and use your verified domain
# Example: "Dudhpuke <no-reply@yourdomain.com>"
RESEND_FROM_EMAIL="Dudhpuke <onboarding@resend.dev>"
RESEND_REPLY_TO="dudhpuke@gmail.com"

# Node Environment
# Automatically set by most deployment platforms
# Options: development, production, test
NODE_ENV="development"
